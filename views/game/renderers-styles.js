import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  // new nerd
  rocket: {
    width: 50,
    height: 100,
    position: "absolute",
  },
  // new computer
  satellite: {
    position: "absolute",
  },
  // new email
  planet: {
    width: 50,
    height: 90,
    position: "absolute",
  },
  // new duck
  ufo: {
    width: 75,
    height: 75,
    position: "absolute",
  },
  star: {
    position: "absolute",
  },
});

export default styles;
